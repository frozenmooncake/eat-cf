name = "eat-cf-worker"
main = "index.js"
compatibility_date = "2024-11-01"

[vars]
# 通过 wrangler secret put UPSTASH_REDIS_REST_URL / UPSTASH_REDIS_REST_TOKEN 配置，
# 或在此处填入明文（不推荐用于生产）。
# UPSTASH_REDIS_REST_URL = "https://xxx.upstash.io"
# UPSTASH_REDIS_REST_TOKEN = "xxx"
